{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Build Code",
            "type": "shell",
            // For windows, uncomment g++ and comment out the linux command
            // "command": "g++",
            // For Linux:
            "command": "i686-w64-mingw32-g++",
            "args": [
                //Compilation Flags and Options
                "-std=c++11", "client.cpp", "-o", "dee33.exe", "-s", "-lws2_32", "-Wno-write-strings", "-fno-exceptions", "-fmerge-all-constants", "-static-libstdc++", "-static-libgcc"
            ],
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}